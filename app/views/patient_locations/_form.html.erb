<%= form_for(@patient_location) do |f| %>
  <% if @patient_location.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@patient_location.errors.count, "error") %> prohibited this patient_location from being saved:</h2>

      <ul>
      <% @patient_location.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :address %><br>
    <%= f.text_field :address %>
  </div>
  <div class="field">
    <%= f.label :latitude %><br>
    <%= f.text_field :latitude %>
  </div>
  <div class="field">
    <%= f.label :longitude %><br>
    <%= f.text_field :longitude %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
